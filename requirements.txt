# Dependencies for the BOSS BC baselines (BC-RESNET-RNN / BC-RESNET-T / BC-VIT-T).
#
# Pinned to the versions the paper's environment ran with. Verified to resolve
# together with `pip install --dry-run` on python 3.10.
#
# OpenVLA is NOT included: it has its own, conflicting dependency set. Install it
# separately -- see integrations/openvla/README.md.
#
# Two entries below are transitive dependencies our own code never imports, but
# without which a clean install fails. Both were found by building the
# environment from scratch, not by reading the source.
bddl==1.0.1
cloudpickle==2.1.0
easydict==1.9
einops==0.4.1
# bddl imports future.utils but does not declare it
future==0.18.2
gym==0.25.2
h5py==3.12.1
hydra-core==1.2.0
huggingface_hub==0.36.2
imageio==2.35.1
imageio-ffmpeg==0.5.1
matplotlib==3.10.0
mujoco==3.2.3
numpy==1.23.1
omegaconf==2.3.0
opencv-python==4.10.0.84
pillow==11.2.1
pytest==8.3.3
PyYAML==6.0.2
robomimic==0.2.0
robosuite==1.4.1
# wandb 0.13.1 imports pkg_resources, dropped by setuptools 81
setuptools==80.10.2
termcolor==2.4.0
thop==0.1.1-2209072238
torch==2.6.0
torchvision==0.21.0
tqdm==4.66.5
transformers==4.48.3
wandb==0.13.1
